# Route all installs through the npm registry
registry=https://registry.npmjs.org/

# Block packages published less than 7 days ago
min-release-age=7

# Disable postinstall and lifecycle scripts from third-party packages
ignore-scripts=true

# Pin exact versions — no ^ or ~ ranges on save
save-exact=true
